package com.ruleflow

import org.junit.Test

class RuleFlowToolWindowFactoryTest {
    @Test
    fun smoke() {
        // Placeholder: UI tests require IntelliJ test framework; this is a skeleton
        // kept intentionally minimal to avoid adding heavy test deps.
        assert(true)
    }
}
